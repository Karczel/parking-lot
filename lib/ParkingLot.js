class ParkingLot {
    constructor() {
    }
    
    park(vehicle) {
        // find space
        //use_space(find_space)
    }

    depart(vehicle){
        // for i in level
        // if parking_lot.vehicle == vehicle
        // remove vehicle from parking_lots
    }

    find_space(type) {
        // return parking_lot as [] for consequtive
        for (i in levels){
            // found any: break loop

            //if type == bus, find con

        // if type == car

        // else if type == motorcycle

        }
        // none spot found; error report
    }

    use_space(parking_lot, vehicle) {
        // for i in parking_lot
        // parking_lot.available = false
        // parking_lot.vehicle = vehicle
    }
}